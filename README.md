# go-doc-service

Example of log service which record folder by date, and have log categories

TODO: Using the basic no need to write to a folder, the archive process of basic function resolve itself.
